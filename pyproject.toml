[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N"]
ignore = ["B008", "E501", "N806"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
  "packages/schemas/src",
  "services/planner/src",
  "apps/api"
]
