# Zenith Schemas

This package contains shared request and response models used across the API and planner.

Phase-zero scope:

- define stable DTO boundaries
- avoid leaking route-local models into planner code
- provide a place for canonical request and response schemas before persistence models arrive

